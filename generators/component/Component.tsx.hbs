import React from 'react'
import clsx from "clsx"

export type {{properCase name}}Props = {
  children?: React.ReactNode
  className?: string
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const {{properCase name}} = React.forwardRef<HTMLDivElement, {{properCase name}}Props>(({className, children, ...rest}, ref) => { 
  return (
    <div {...rest} className={clsx(className, '{{dashCase name}}', '')} ref={ref}>
      {children}
    </div>
  )
})